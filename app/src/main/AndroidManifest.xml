<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/spicone"
        android:label="@string/app_name"
        android:roundIcon="@drawable/spicone"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportsRealsbe"
        tools:targetApi="31">
        <activity
            android:name=".SportWebActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|keyboard|fontScale|smallestScreenSize|screenLayout|touchscreen|locale"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SportsRealsbe">
        </activity>
    </application>

</manifest>